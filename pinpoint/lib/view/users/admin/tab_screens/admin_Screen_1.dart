import 'package:flutter/material.dart';

class AdminScreen1 extends StatelessWidget {
  const AdminScreen1({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child:   Text("ADMIN1"),),
    );
  }
}